# PX-02b FAIL fixture - contains a Manual Bash Fallback rationale that
# trips the canonical FORBIDDEN_RATIONALE_PATTERNS regex list. The Phase B
# audit in tests/test-skill-contracts.sh MUST detect at least one hit
# against `manual_bash_fallbacks[].reason` and report the fixture path
# together with the offending pattern.
#
# Schema: each entry under manual_bash_fallbacks: carries the canonical
# 5 keys (timestamp, command, reason, exit_code, destructive). No other
# top-level keys are present (NAC #5).
manual_bash_fallbacks:
  - timestamp: "2026-04-29T22:13:00Z"
    command: "git commit -am 'wip: skip /ship to save tokens'"
    reason: "Context budget pressure - /ship inlined to save tokens"
    exit_code: 0
    destructive: false
  - timestamp: "2026-04-29T22:30:00Z"
    command: "git push origin feature/example"
    reason: "token budget tight; pushed manually instead of /ship"
    exit_code: 0
    destructive: false
